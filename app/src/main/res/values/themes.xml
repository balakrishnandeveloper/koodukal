<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.MyAndroidApplication"
        parent="Theme.Material3.Light.NoActionBar">

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>

        <item name="android:windowLightStatusBar">true</item>

        <item name="bottomNavigationStyle">@style/CustomBottomNav</item>
        <!-- White status bar -->
        <item name="android:statusBarColor">@android:color/white</item>

    </style>

    <style name="Theme.MyAndroidApplication"
        parent="Base.Theme.MyAndroidApplication" />

    <style name="edittext_style_without_drawable">
        <item name="android:singleLine">true</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/editext_bg</item>
    </style>

    <style name="CustomBottomNav"
        parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
    </style>

    <style name="MyDatePickerTheme"
        parent="ThemeOverlay.Material3.MaterialAlertDialog">

        <item name="colorPrimary">@color/app_color</item>
        <item name="colorSecondary">@color/app_color</item>

    </style>

</resources>